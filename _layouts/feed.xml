---
layout: none
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <link href="{{ site.url }}/feed.xml" rel="self"/>
  <link href="{{ site.url }}"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/</id>

  {% for post in site.posts limit:20 %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>

    {% if post.source %}
      <link href="{{ post.source | xml_escape }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
      <id>{{ post.source | xml_escape }}</id>
    {% else %}
      <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
      <id>{{ site.url }}{{ post.url }}</id>
    {% endif %}

    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>

    <summary type="html">
      <![CDATA[ {{ post.excerpt | strip_html | xml_escape }} ]]>
    </summary>

    <content type="html">
      <![CDATA[ {{ post.content }} ]]>
    </content>
  </entry>
  {% endfor %}
</feed>
