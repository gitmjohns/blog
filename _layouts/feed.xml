---
layout: nil
---

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title | xml_escape }}</title>
  <link href="{{ site.url }}/feed.xml" rel="self"/>
  <link href="{{ site.url }}"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}</id>
  <author>
    <name>{{ site.author | default: site.title }}</name>
  </author>

  {% for post in site.posts limit:20 %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>

    {% if post.source_url %}
      <link href="{{ post.source_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
      <id>{{ post.source_url }}</id>
    {% else %}
      <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
      <id>{{ site.url }}{{ post.id }}</id>
    {% endif %}

    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>

    <content type="html" xml:base="{% if post.source_url %}{{ post.source_url }}{% else %}{{ site.url }}{{ post.url }}{% endif %}">
      <![CDATA[
        {{ post.content | strip_html | truncatewords: 50 }}
        <p><a href="{% if post.source_url %}{{ post.source_url }}{% else %}{{ site.url }}{{ post.url }}{% endif %}">Read the full article</a></p>
      ]]>
    </content>

    <summary type="html">
      <![CDATA[
        {{ post.content | strip_html | truncatewords: 30 }}
      ]]>
    </summary>

    <author><name/></author>
    {% if post.categories %}
    <category term="{{ post.categories | join: ',' }}"/>
    {% endif %}
  </entry>
  {% endfor %}
</feed>
